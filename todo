
Todo:
  - If user already logged in then logged out, check for 'one click login', else option to log in as diff user

We should cache user.feed on login and give the user a button to manually refresh it

Disable submit once algorithm is running

make oauth an ajax call so that bgcolor can fade from default to user ave_color

make sure site looks ok if bgpic is lightcolored

footer-colored border around user.feed box
